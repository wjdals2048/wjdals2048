#!/bin/bash

# 내부 함수 정의
run_ls() {
    echo "프로그램을 시작합니다."
    echo "함수 안으로 들어옴"
    # 인자가 없을 경우 현재 디렉토리를 리스트하도록 기본 동작 설정
    if [ "$#" -eq 0 ]; then
        ls .
    else
        ls "$@"
    fi
    echo "함수 안에서 나옴"
    echo "프로그램을 종료합니다."
}

# 함수 호출, 인자를 함수에 전달
run_ls "$@"
